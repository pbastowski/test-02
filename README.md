# Test angular2-now NPM package with SystemJS and TypeScript

Install npm dependencies.

    npm install

Run the live-server, which will open a new tab in your browser automatically.

    npm run serve

